{
  "name": "grid-layout-test",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bundle": "parcel build --public-url /grid-layout-test src/index.html",
    "deploy": "gh-pages --dist dist --message \"Update website\"",
    "start": "parcel --out-dir build src/index.html"
  },
  "devDependencies": {
    "autoprefixer": "^9.1.5",
    "gh-pages": "^2.0.1",
    "parcel": "^1.10.3"
  }
}
